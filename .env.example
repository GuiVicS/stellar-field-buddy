# ============================================
# Stellar Print — Variáveis de Ambiente
# Copie para .env e preencha os valores
# ============================================

# Senha do banco PostgreSQL
POSTGRES_PASSWORD=sua-senha-forte-aqui

# JWT Secret (mínimo 32 caracteres)
# Gere com: openssl rand -base64 32
JWT_SECRET=super-secret-jwt-token-com-pelo-menos-32-caracteres

# Supabase Keys (gere com o JWT_SECRET acima)
# Use: https://supabase.com/docs/guides/self-hosting#api-keys
ANON_KEY=sua-anon-key-aqui
SERVICE_ROLE_KEY=sua-service-role-key-aqui

# URLs
API_EXTERNAL_URL=http://localhost:8000
SITE_URL=http://localhost:3000
